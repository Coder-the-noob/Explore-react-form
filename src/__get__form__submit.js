/**
 * 1.e.target.[name of the input field].value to get the value from form submit event
 * 2.FormData.get('name') to get the value from formData object
 * 3.Controlled components using useState to manage form input values, one per input field
 * 3. handleSubmit function to prevent default form submission and handle form data
 * 
 * 4. uncontrolled components using useRef to access form input values directly
 * 
 * 
*/